services:
    _defaults:
        bind:
            $quz: value
            $foo: [value]

    bar:
        class: Symfony\Component\Dependency_Injection\Tests\Fixtures\Bar

    foo:
        class: stdClass
